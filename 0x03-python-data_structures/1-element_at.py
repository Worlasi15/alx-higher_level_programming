#!/usr/bin/python3
# 1-element_at.py


def element_at(my_list, idx):
	for item in my_list
    if idx < 0 or idx > (len(my_list) - 1):
	    return None
	elif idx == 0:
	       return item
	       idx -= 1
